{#
/**
 * @file
 * Contains Drupal\mailchimp_overlay\templates\mailchimp-overlay-fixed-bottom-bar.html.twig.
 * Mailchimp Overlay Fixed Bottom Bar Template.
 */
#}
<div id="mailchimp_overlay fixed_bottom_bar">
	<div class="content">
		<h2 class="title">{{title}}</h2>
			{{ signupform }}
	{% if disclaimer %}
		<p class="text">{{ disclaimer }}</p>
	{% endif %}
</div>
</div>